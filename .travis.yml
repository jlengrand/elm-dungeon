language: node_js
node_js:
  - "iojs"
  - "7"
cache:
  yarn: true
  directories:
    - elm-stuff
    - node_modules
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version version-number
  - export PATH="$HOME/.yarn/bin:$PATH"
script: 
  - yarn
  - yarn prod